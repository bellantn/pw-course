// const myName = "Alex"; - dùng const để khai báo khi biến KHÔNG gán lại
let myName = "Alex"; // => nên dùng let vì biến sẽ được gán lại
myName = "Nagi"; // biến myName được gán lại

console.log(myName);